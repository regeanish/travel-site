// lazysizes package runs on its own. So its better to write in a separate JS File
// Also, Configuration in webpack.config.js is required for automation

import "../../temp/scripts/modernizr";
import 'picturefill';
import 'lazysizes';
